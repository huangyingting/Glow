# The default Open-Meteo integration requires no API key.
# Set this only if you later add a commercial high-resolution provider.
WEATHER_PROVIDER_KEY=

# Optional MapLibre style. The default is the keyless CARTO / OpenStreetMap raster style.
NEXT_PUBLIC_MAP_STYLE_URL=
